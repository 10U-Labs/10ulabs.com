---
concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow_ref }}-${{ github.ref }}
jobs:
  deploy:
    if: github.ref == 'refs/heads/main'
    name: Deploy /v1/runners endpoint
    permissions:
      actions: write
      contents: read
      id-token: write
    runs-on: >-
      ${{ ( vars.USE_GITHUB_HOSTED == 'true' ||
            github.event.inputs.github_hosted == 'true' ||
            contains(github.event.head_commit.message, '[github-hosted]') ) &&
          'ubuntu-latest' ||
          fromJSON(format('["ecs", "fargate", "arm", "spot", "runner-{0}"]',
            github.run_id)) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - id: config
        name: Extract configuration values
        run: |
          TF_SHARED_LOCALS=lib/terraform/modules/shared/locals.tf
          LOCALS=src/bootstrap/locals.tf
          BACKEND=src/api/endpoints/runners/backend.tf
          AWS_REGION=$(grep 'aws_region' $TF_SHARED_LOCALS \
            | grep -v '\$' | cut -d'"' -f2)
          AWS_ACCOUNT_ID=$(grep 'aws_account_id' $TF_SHARED_LOCALS \
            | grep -v '\$' | cut -d'"' -f2)
          RESOURCE_PREFIX=$(grep 'resource_prefix' $TF_SHARED_LOCALS \
            | grep -v '\$' | cut -d'"' -f2)
          ROLE_SUFFIX=$(grep 'name_for_github_actions_role' $LOCALS \
            | sed 's/.*\${local\.resource_prefix}//' | cut -d'"' -f1)
          ROLE_ARN="arn:aws:iam::${AWS_ACCOUNT_ID}:role/"
          ROLE_ARN="${ROLE_ARN}${RESOURCE_PREFIX}${ROLE_SUFFIX}"
          TERRAFORM_VERSION=$(grep 'required_version' $BACKEND \
            | cut -d'"' -f2 | sed 's/>= //')
          echo "aws_region=$AWS_REGION" >> $GITHUB_OUTPUT
          echo "role_arn=$ROLE_ARN" >> $GITHUB_OUTPUT
          echo "terraform_version=$TERRAFORM_VERSION" >> $GITHUB_OUTPUT
      - if: >-
          vars.USE_GITHUB_HOSTED == 'true' ||
          github.event.inputs.github_hosted == 'true' ||
          contains(github.event.head_commit.message, '[github-hosted]')
        name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ steps.config.outputs.terraform_version }}
          terraform_wrapper: false
      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ steps.config.outputs.aws_region }}
          role-to-assume: ${{ steps.config.outputs.role_arn }}
      - if: >-
          vars.USE_GITHUB_HOSTED == 'true' ||
          github.event.inputs.github_hosted == 'true' ||
          contains(github.event.head_commit.message, '[github-hosted]')
        name: Setup TFLint
        uses: terraform-linters/setup-tflint@v6
        with:
          tflint_version: latest
      - if: >-
          vars.USE_GITHUB_HOSTED == 'true' ||
          github.event.inputs.github_hosted == 'true' ||
          contains(github.event.head_commit.message, '[github-hosted]')
        name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - if: >-
          vars.USE_GITHUB_HOSTED == 'true' ||
          github.event.inputs.github_hosted == 'true' ||
          contains(github.event.head_commit.message, '[github-hosted]')
        name: Install Python dependencies
        run: |
          python3 -m pip install pylint mypy yamllint pytest boto3 \
            boto3-stubs PyYAML types-PyYAML requests types-requests
      - if: >-
          vars.USE_GITHUB_HOSTED == 'true' ||
          github.event.inputs.github_hosted == 'true' ||
          contains(github.event.head_commit.message, '[github-hosted]')
        name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Linting YAML files
        run: |
          EMPTY_LINES='empty-lines: {max: 0, max-start: 0, max-end: 1}'
          KEY_ORDERING='key-ordering: enable'
          NEWLINE_AT_EOF='new-line-at-end-of-file: enable'
          TRUTHY='truthy: {allowed-values: ["true", "false", "on"]}'
          RULES="$EMPTY_LINES, $KEY_ORDERING"
          RULES="$RULES, $NEWLINE_AT_EOF, $TRUTHY"
          python3 -m yamllint --strict \
            --config-data "{extends: default, rules: {$RULES}}" \
            .github/workflows/endpoint_v1_runners.yml
      - name: Linting Python files
        run: |
          for file in src/api/endpoints/runners/lambdas/*.py; do
            PYTHONPATH=lib/python python3 -m pylint "$file" \
              --fail-on=C,R,W \
              --fail-under=10.0
          done
          PYTHONPATH=lib/python python3 -m pylint test/api/endpoints/runners/ \
            --fail-on=C,R,W \
            --fail-under=10.0
      - name: Static type checking Python files with mypy
        run: |
          for file in src/api/endpoints/runners/lambdas/*.py; do
            MYPYPATH=lib/python python3 -m mypy "$file"
          done
          MYPYPATH=lib/python python3 -m mypy test/api/endpoints/runners/
      - if: >-
          vars.USE_GITHUB_HOSTED == 'true' ||
          github.event.inputs.github_hosted == 'true' ||
          contains(github.event.head_commit.message, '[github-hosted]')
        name: Install ESLint dependencies
        run: npm install --save-dev @eslint/js globals
      - name: Linting JavaScript files
        run: |
          npx eslint -c etc/eslint.config.mjs \
            src/api/endpoints/runners/lambdas/
          cd test/api/endpoints/runners
          npm ci
          npm run lint
      - name: Linting JSON files
        run: |
          for file in \
            src/api/endpoints/runners/lambdas/layer/package.json \
            test/api/endpoints/runners/package.json
          do
            echo "Validating $file"
            jsonlint -q "$file"
          done
      - name: Terraform Format Check
        run: |
          cd src/api/endpoints/runners
          terraform fmt -diff -check -recursive
      - name: Terraform Init
        run: cd src/api/endpoints/runners && terraform init
      - env:
          GITHUB_TOKEN: ${{ github.token }}
        name: Run tflint
        run: cd src/api/endpoints/runners && tflint --init && tflint
      - name: Terraform Plan
        run: cd src/api/endpoints/runners && terraform plan
      - name: Run Python unit tests
        run: |
          python3 -m pytest --durations=0 --durations=0 \
            test/api/endpoints/runners/pre_deployment/unit/ \
            --ignore=test/api/endpoints/runners/pre_deployment/unit/*.test.js \
            --confcutdir=test --verbose --pythonwarnings=error
      - name: Run JavaScript unit tests
        run: |
          cd test/api/endpoints/runners
          npm ci
          npm test
      - name: Run pre-deployment integration tests
        run: |
          TEST=test/api/endpoints/runners/pre_deployment/integration
          python3 -m pytest --durations=0 --durations=0 \
            $TEST/ \
            --capture=no --confcutdir=test --verbose --pythonwarnings=error
      - name: Terraform Apply
        run: cd src/api/endpoints/runners && terraform apply -auto-approve
      - name: Run post-deployment integration tests
        run: |
          TEST=test/api/endpoints/runners/post_deployment/integration
          python3 -m pytest --durations=0 --durations=0 \
            $TEST/ \
            --confcutdir=test --verbose --pythonwarnings=error
      - env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        name: Disable USE_GITHUB_HOSTED variable
        run: |
          echo "Runners workflow completed successfully."
          echo "Disabling USE_GITHUB_HOSTED to switch future workflows to ECS."
          gh variable set USE_GITHUB_HOSTED --body "false" \
            --repo ${{ github.repository }}
      - env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        if: >-
          github.event_name == 'workflow_dispatch' &&
          github.event.inputs.skip_descendants != 'true' &&
          !contains(github.event.head_commit.message, '[skip descendants]')
        name: Dispatch descendant workflows
        run: |
          SKIP_FLAG=""
          SKIP_INPUT="${{ github.event.inputs.skip_descendants }}"
          if [ "$SKIP_INPUT" = "true" ]; then SKIP_FLAG="--skip-descendants"; fi
          python3 scripts/dispatch_descendants.py \
            $SKIP_FLAG \
            --workflow endpoint_v1_runners \
            --repo ${{ github.repository }}
name: Ensuring the /v1/runners API endpoint exists and is properly configured
on:
  workflow_dispatch:
    inputs:
      github_hosted:
        default: false
        description: 'Use GitHub-hosted runner instead of ephemeral ECS'
        type: boolean
      skip_descendants:
        default: false
        description: Skip dispatching descendant workflows
        type: boolean
      spot_recovery_reason:
        description: Spot recovery trigger reason
        required: false
        type: string
permissions:
  actions: write
  contents: read
  id-token: write
