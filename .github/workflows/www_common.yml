---
concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow_ref }}-${{ github.ref }}
jobs:
  deploy:
    if: github.ref == 'refs/heads/main'
    name: Deploy www_common infrastructure
    runs-on: >-
      ${{ ( vars.WORKFLOWS_MUST_USE_GITHUB_HOSTED_RUNNERS == 'true' ||
            github.event.inputs.github_hosted == 'true' ||
            contains(github.event.head_commit.message, '[github-hosted]') ) &&
          'ubuntu-latest' ||
          fromJSON(format('["ecs", "fargate", "arm", "spot", "runner-{0}"]',
            github.run_id)) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - continue-on-error: true
        id: no_linter_configs_gh
        if: vars.WORKFLOWS_MUST_USE_GITHUB_HOSTED_RUNNERS == 'true'
        name: Assert no linter config files (GitHub-hosted)
        uses: 10U-Labs-LLC/assert-no-linter-config-files@latest
        with:
          directories: ${{ github.workspace }}
          linters: pylint,mypy,yamllint
          verbose: true
      - continue-on-error: true
        id: no_linter_configs_sh
        if: vars.WORKFLOWS_MUST_USE_GITHUB_HOSTED_RUNNERS != 'true'
        name: Assert no linter config files (self-hosted)
        run: >-
          assert-no-linter-config-files
          --linters pylint,mypy,yamllint
          --verbose
          ${{ github.workspace }}
      - continue-on-error: true
        id: no_inline_disables_gh
        if: vars.WORKFLOWS_MUST_USE_GITHUB_HOSTED_RUNNERS == 'true'
        name: Assert no inline lint disables (GitHub-hosted)
        uses: 10U-Labs-LLC/assert-no-inline-directives@latest
        with:
          files: ${{ github.workspace }}/**/*
          tools: pylint,mypy,yamllint
          verbose: true
      - continue-on-error: true
        id: no_inline_disables_sh
        if: vars.WORKFLOWS_MUST_USE_GITHUB_HOSTED_RUNNERS != 'true'
        name: Assert no inline lint disables (self-hosted)
        run: >-
          assert-no-inline-directives
          --tools pylint,mypy,yamllint
          --verbose
          ${{ github.workspace }}/**/*
      - continue-on-error: true
        id: one_assert_per_pytest_gh
        if: vars.WORKFLOWS_MUST_USE_GITHUB_HOSTED_RUNNERS == 'true'
        name: Assert one assert per pytest (GitHub-hosted)
        uses: 10U-Labs-LLC/assert-one-assert-per-pytest@latest
        with:
          files: ${{ github.workspace }}/test/**/*.py
          verbose: true
      - continue-on-error: true
        id: one_assert_per_pytest_sh
        if: vars.WORKFLOWS_MUST_USE_GITHUB_HOSTED_RUNNERS != 'true'
        name: Assert one assert per pytest (self-hosted)
        run: >-
          assert-one-assert-per-pytest
          --verbose
          ${{ github.workspace }}/test
      - name: Verify OIDC variables
        uses: ./.github/actions/verify-oidc-vars
        with:
          aws_region: ${{ vars.AWS_REGION }}
          oidc_role_arn: ${{ vars.OIDC_ROLE_ARN }}
      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ vars.AWS_REGION }}
          role-to-assume: ${{ vars.OIDC_ROLE_ARN }}
      - if: >-
          vars.WORKFLOWS_MUST_USE_GITHUB_HOSTED_RUNNERS == 'true' ||
          github.event.inputs.github_hosted == 'true' ||
          contains(github.event.head_commit.message, '[github-hosted]')
        name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - if: >-
          vars.WORKFLOWS_MUST_USE_GITHUB_HOSTED_RUNNERS == 'true' ||
          github.event.inputs.github_hosted == 'true' ||
          contains(github.event.head_commit.message, '[github-hosted]')
        name: Install Python dependencies
        run: |
          python3 -m pip install \
            boto3 \
            boto3-stubs \
            botocore \
            dnspython \
            mypy \
            pylint \
            pytest \
            pytest-dependency \
            python-hcl2 \
            requests \
            types-PyYAML \
            types-requests \
            yamllint
      - continue-on-error: true
        id: yamllint
        name: Linting YAML files
        run: |
          EMPTY_LINES='empty-lines: {max: 0, max-start: 0, max-end: 1}'
          KEY_ORDERING='key-ordering: enable'
          NEWLINE_AT_EOF='new-line-at-end-of-file: enable'
          TRUTHY='truthy: {allowed-values: ["true", "false", "on"]}'
          RULES="$EMPTY_LINES, $KEY_ORDERING"
          RULES="$RULES, $NEWLINE_AT_EOF, $TRUTHY"
          python3 -m yamllint --strict \
            --config-data "{extends: default, rules: {$RULES}}" \
            .github/workflows/www_common.yml
      - continue-on-error: true
        id: pylint_source
        name: Run pylint on source
        run: |
          python3 -m pylint \
            src/www/common/lambda/ \
            --fail-on=C,R,W \
            --fail-under=10.0
      - continue-on-error: true
        id: pylint_tests
        name: Run pylint on tests
        run: |
          PYTHONPATH=lib/python python3 -m pylint \
            lib/python/ test/ \
            --fail-on=C,R,W \
            --fail-under=10.0
      - continue-on-error: true
        id: mypy_source
        name: Run mypy on source
        run: |
          python3 -m mypy \
            src/www/common/lambda/
      - continue-on-error: true
        id: mypy_tests
        name: Run mypy on tests
        run: |
          MYPYPATH=lib/python:src/api/endpoints/runners/lambda \
            python3 -m mypy \
            lib/python/ test/
      - if: >-
          vars.WORKFLOWS_MUST_USE_GITHUB_HOSTED_RUNNERS == 'true' ||
          github.event.inputs.github_hosted == 'true' ||
          contains(github.event.head_commit.message, '[github-hosted]')
        name: Install jscpd
        run: npm install -g jscpd
      - continue-on-error: true
        id: jscpd_source
        name: Check for duplicate code in source Python files
        run: |
          jscpd --pattern "**/*.py" --threshold 0 --reporters console \
            src/www/common/lambda/
      - continue-on-error: true
        id: jscpd_tests
        name: Check for duplicate code in test Python files
        run: |
          jscpd --pattern "**/*.py" --threshold 0 --reporters console \
            lib/python/ test/
      - id: terraform
        if: >-
          vars.WORKFLOWS_MUST_USE_GITHUB_HOSTED_RUNNERS == 'true' ||
          github.event.inputs.github_hosted == 'true' ||
          contains(github.event.head_commit.message, '[github-hosted]')
        name: Extract Terraform version
        run: |
          TERRAFORM_VERSION=$(grep 'required_version' \
            src/www/common/backend.tf | cut -d'"' -f2 | sed 's/>= //')
          echo "version=$TERRAFORM_VERSION" >> $GITHUB_OUTPUT
      - if: >-
          vars.WORKFLOWS_MUST_USE_GITHUB_HOSTED_RUNNERS == 'true' ||
          github.event.inputs.github_hosted == 'true' ||
          contains(github.event.head_commit.message, '[github-hosted]')
        name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ steps.terraform.outputs.version }}
          terraform_wrapper: false
      - continue-on-error: true
        id: terraform_fmt
        name: Terraform Format Check
        run: cd src/www/common && terraform fmt -diff -check -recursive
      - continue-on-error: true
        id: terraform_fmt_s3_bucket
        name: Terraform Format Check on s3_bucket module
        run: cd lib/terraform/s3_bucket && terraform fmt -diff -check
      - continue-on-error: true
        id: terraform_fmt_cloudfront_waf
        name: Terraform Format Check on cloudfront_waf module
        run: |
          cd lib/terraform/cloudfront_waf && \
            terraform fmt -diff -check
      - continue-on-error: true
        id: terraform_init
        name: Terraform Init
        run: cd src/www/common && terraform init
      - if: >-
          vars.WORKFLOWS_MUST_USE_GITHUB_HOSTED_RUNNERS == 'true' ||
          github.event.inputs.github_hosted == 'true' ||
          contains(github.event.head_commit.message, '[github-hosted]')
        name: Setup TFLint
        uses: terraform-linters/setup-tflint@v6
        with:
          tflint_version: latest
      - continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ github.token }}
        id: tflint
        name: Run tflint
        run: cd src/www/common && tflint --init && tflint
      - continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ github.token }}
        id: tflint_s3_bucket
        name: Run tflint on s3_bucket module
        run: cd lib/terraform/s3_bucket && tflint --init && tflint
      - continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ github.token }}
        id: tflint_cloudfront_waf
        name: Run tflint on cloudfront_waf module
        run: cd lib/terraform/cloudfront_waf && tflint --init && tflint
      - continue-on-error: true
        id: module_unit_tests
        name: Run module unit tests
        run: |
          python3 -m pytest \
            test/lib/terraform/s3_bucket/unit/ \
            test/lib/terraform/cloudfront_waf/unit/ \
            test/lib/terraform/write_ahead_queue/unit/ \
            test/lib/terraform/common/unit/ \
            --verbose --pythonwarnings=error
      - continue-on-error: true
        id: unit_tests
        name: Run unit tests
        run: |
          python3 -m pytest \
            test/www/common/pre_deployment/unit/ \
            --verbose --pythonwarnings=error
      - continue-on-error: true
        id: pre_integration_tests
        name: Run pre-deployment integration tests
        run: |
          python3 -m pytest \
            test/www/common/pre_deployment/integration/ \
            --capture=no --verbose --pythonwarnings=error
      - continue-on-error: true
        id: terraform_apply
        name: Terraform Apply
        run: cd src/www/common && terraform apply -auto-approve
      - id: check_files
        name: Check if CloudFront config changed
        run: |
          set -e
          set -o pipefail
          if git cat-file -e ${{ github.event.before }} 2>/dev/null; then
            BASE_COMMIT="${{ github.event.before }}"
          else
            BASE_COMMIT="HEAD^"
          fi
          PATTERN='src/www/common/(cloudfront_s3|lambda_edge)\.tf'
          PATTERN="$PATTERN|src/www/common/lambda/"
          git diff --name-only $BASE_COMMIT ${{ github.sha }} | \
            if grep -qE "$PATTERN"; then
              echo "changed=true" >> $GITHUB_OUTPUT
            else
              echo "changed=false" >> $GITHUB_OUTPUT
            fi
      - if: >-
          github.event.inputs.invalidate_cloudfront == 'true' ||
          contains(github.event.head_commit.message,
            '[invalidate cloudfront]') ||
          steps.check_files.outputs.changed == 'true'
        name: Get website domain from Terraform output
        run: |
          WEBSITE_FQDN=$(cd src/www/common && \
            terraform output -raw website_domain_name)
          echo "WEBSITE_FQDN=$WEBSITE_FQDN" >> $GITHUB_ENV
      - if: >-
          github.event.inputs.invalidate_cloudfront == 'true' ||
          contains(github.event.head_commit.message,
            '[invalidate cloudfront]') ||
          steps.check_files.outputs.changed == 'true'
        name: Invalidate CloudFront cache
        run: |
          python3 scripts/invalidate_cloudfront.py \
            --fqdn "${{ env.WEBSITE_FQDN }}" \
            --region "${{ vars.AWS_REGION }}" \
            --paths "/*"
      - continue-on-error: true
        id: post_integration_tests
        name: Run post-deployment integration tests
        run: |
          python3 -m pytest \
            test/www/common/post_deployment/integration/ \
            --verbose --pythonwarnings=error
      - continue-on-error: true
        id: e2e_tests
        name: Run E2E tests
        run: |
          python3 -m pytest \
            test/www/common/post_deployment/e2e/ \
            --verbose --pythonwarnings=error
      - env:
          S02: ${{ steps.no_linter_configs_sh.outcome }}
          S03: ${{ steps.no_inline_disables_gh.outcome }}
          S04: ${{ steps.no_inline_disables_sh.outcome }}
          S05: ${{ steps.one_assert_per_pytest_gh.outcome }}
          S06: ${{ steps.one_assert_per_pytest_sh.outcome }}
          S07: ${{ steps.yamllint.outcome }}
          S08: ${{ steps.pylint_source.outcome }}
          S09: ${{ steps.pylint_tests.outcome }}
          S10: ${{ steps.mypy_source.outcome }}
          S11: ${{ steps.mypy_tests.outcome }}
          S12: ${{ steps.jscpd_source.outcome }}
          S13: ${{ steps.jscpd_tests.outcome }}
          S14: ${{ steps.terraform_fmt.outcome }}
          S15: ${{ steps.terraform_fmt_s3_bucket.outcome }}
          S16: ${{ steps.terraform_fmt_cloudfront_waf.outcome }}
          S17: ${{ steps.terraform_init.outcome }}
          S18: ${{ steps.tflint.outcome }}
          S19: ${{ steps.tflint_s3_bucket.outcome }}
          S20: ${{ steps.tflint_cloudfront_waf.outcome }}
          S21: ${{ steps.module_unit_tests.outcome }}
          S22: ${{ steps.unit_tests.outcome }}
          S23: ${{ steps.pre_integration_tests.outcome }}
          S24: ${{ steps.terraform_apply.outcome }}
          S25: ${{ steps.post_integration_tests.outcome }}
          S26: ${{ steps.e2e_tests.outcome }}
        name: Assert no step failed
        run: |
          failed=""
          f="failure"
          [ "$S01" = "$f" ] && failed="$failed no_linter_configs_gh"
          [ "$S02" = "$f" ] && failed="$failed no_linter_configs_sh"
          [ "$S03" = "$f" ] && failed="$failed no_inline_disables_gh"
          [ "$S04" = "$f" ] && failed="$failed no_inline_disables_sh"
          [ "$S05" = "$f" ] && failed="$failed one_assert_per_pytest_gh"
          [ "$S06" = "$f" ] && failed="$failed one_assert_per_pytest_sh"
          [ "$S07" = "$f" ] && failed="$failed yamllint"
          [ "$S08" = "$f" ] && failed="$failed pylint_source"
          [ "$S09" = "$f" ] && failed="$failed pylint_tests"
          [ "$S10" = "$f" ] && failed="$failed mypy_source"
          [ "$S11" = "$f" ] && failed="$failed mypy_tests"
          [ "$S12" = "$f" ] && failed="$failed jscpd_source"
          [ "$S13" = "$f" ] && failed="$failed jscpd_tests"
          [ "$S14" = "$f" ] && failed="$failed terraform_fmt"
          [ "$S15" = "$f" ] && failed="$failed terraform_fmt_s3_bucket"
          [ "$S16" = "$f" ] && failed="$failed terraform_fmt_cloudfront_waf"
          [ "$S17" = "$f" ] && failed="$failed terraform_init"
          [ "$S18" = "$f" ] && failed="$failed tflint"
          [ "$S19" = "$f" ] && failed="$failed tflint_s3_bucket"
          [ "$S20" = "$f" ] && failed="$failed tflint_cloudfront_waf"
          [ "$S21" = "$f" ] && failed="$failed module_unit_tests"
          [ "$S22" = "$f" ] && failed="$failed unit_tests"
          [ "$S23" = "$f" ] && failed="$failed pre_integration_tests"
          [ "$S24" = "$f" ] && failed="$failed terraform_apply"
          [ "$S25" = "$f" ] && failed="$failed post_integration_tests"
          [ "$S26" = "$f" ] && failed="$failed e2e_tests"
          if [ -n "$failed" ]; then
            echo "Steps that failed:$failed"
            exit 1
          fi
          echo "All steps succeeded"
      - env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        if: >-
          github.event_name == 'workflow_dispatch' &&
          (
          github.event.inputs.trigger_descendants == 'true' ||
           contains(github.event.head_commit.message,
           '[trigger descendants]'))
        name: Dispatch descendant workflows
        run: |
          FLAGS=""
          TRIGGER="${{ github.event.inputs.trigger_descendants }}"
          INVALIDATE="${{ github.event.inputs.invalidate_cloudfront }}"
          if [ "$TRIGGER" = "true" ]; then
            FLAGS="$FLAGS --trigger-descendants"
          fi
          if [ "$INVALIDATE" = "true" ]; then
            FLAGS="$FLAGS --invalidate-cloudfront"
          fi
          python3 src/workflowctl/workflowctl.py dispatch-descendant-workflows \
            --workflow www_common \
            --repo ${{ github.repository }} \
            $FLAGS
name: Ensuring www common infrastructure exists and is properly configured
on:
  workflow_dispatch:
    inputs:
      github_hosted:
        default: false
        description: 'Use GitHub-hosted runner instead of ephemeral ECS'
        type: boolean
      invalidate_cloudfront:
        default: false
        description: Force CloudFront cache invalidation
        type: boolean
      spot_recovery_reason:
        description: Spot recovery trigger reason
        required: false
        type: string
      trigger_descendants:
        default: false
        description: Trigger descendant workflows after this workflow completes
        type: boolean
permissions:
  actions: write
  contents: read
  id-token: write
