concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow_ref }}-${{ github.ref }}
jobs:
  declaring-common-variables:
    if: github.ref == 'refs/heads/main'
    name: Declaring common variables
    outputs:
      aws_region: ${{ steps.shared.outputs.aws_region }}
      github_actions_role_arn: ${{ steps.shared.outputs.github_actions_role_arn }}
    runs-on:
      - ecs-fargate-spot
      - runner-${{ github.run_id }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - id: shared
        name: Extract shared configuration values
        run: |
          AWS_REGION=$(grep -A1 'output "aws_region"' lib/terraform/outputs.tf | grep 'value' | cut -d'"' -f2)
          AWS_ACCOUNT_ID=$(grep -A1 'output "aws_account_id"' lib/terraform/outputs.tf | grep 'value' | cut -d'"' -f2)
          RESOURCE_PREFIX=$(grep -A1 'output "resource_prefix"' lib/terraform/outputs.tf | grep 'value' | cut -d'"' -f2)
          GITHUB_ACTIONS_ROLE_SUFFIX=$(grep 'name_for_github_actions_role' src/bootstrap/locals.tf | sed 's/.*\${local\.resource_prefix}//' | cut -d'"' -f1)
          GITHUB_ACTIONS_ROLE_ARN="arn:aws:iam::${AWS_ACCOUNT_ID}:role/${RESOURCE_PREFIX}${GITHUB_ACTIONS_ROLE_SUFFIX}"
          echo "aws_region=$AWS_REGION" >> $GITHUB_OUTPUT
          echo "github_actions_role_arn=$GITHUB_ACTIONS_ROLE_ARN" >> $GITHUB_OUTPUT
  ensuring-endpoint-infrastructure-in-desired-state:
    name: Ensuring endpoint infrastructure is in desired state
    needs:
      - declaring-common-variables
      - unit-testing
    permissions:
      contents: read
      id-token: write
    runs-on:
      - ecs-fargate-spot
      - runner-${{ github.run_id }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ needs.declaring-common-variables.outputs.aws_region }}
          role-to-assume: ${{ needs.declaring-common-variables.outputs.github_actions_role_arn }}
      - name: Terraform Init
        run: cd src/api/endpoints/ec2_runner && terraform init
      - name: Terraform Apply
        run: cd src/api/endpoints/ec2_runner && terraform apply -auto-approve
  integration-testing:
    name: Integration testing
    needs:
      - declaring-common-variables
      - ensuring-endpoint-infrastructure-in-desired-state
    permissions:
      contents: read
      id-token: write
    runs-on:
      - ecs-fargate-spot
      - runner-${{ github.run_id }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ needs.declaring-common-variables.outputs.aws_region }}
          role-to-assume: ${{ needs.declaring-common-variables.outputs.github_actions_role_arn }}
      - name: Run integration tests
        run: |
          pytest test/api/endpoints/ec2_runner/post_deployment/integration/ \
            --confcutdir=test/api/endpoints/ec2_runner \
            -v
  performing-static-analysis:
    name: Performing static analysis
    needs: declaring-common-variables
    runs-on:
      - ecs-fargate-spot
      - runner-${{ github.run_id }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Linting YAML files
        run: |
          DOCUMENT_START='document-start: disable'
          EMPTY_LINES='empty-lines: {max: 0, max-start: 0, max-end: 1}'
          KEY_ORDERING='key-ordering: enable'
          LINE_LENGTH='line-length: {max: 300, level: warning}'
          NEWLINE_AT_EOF='new-line-at-end-of-file: enable'
          TRUTHY='truthy: {allowed-values: ["true", "false", "on"]}'
          RULES="$DOCUMENT_START, $EMPTY_LINES, $KEY_ORDERING, $LINE_LENGTH, $NEWLINE_AT_EOF, $TRUTHY"
          yamllint \
            --config-data "{extends: default, rules: {$RULES}}" \
            .github/workflows/ec2_runner.yml
      - name: Linting Python files
        run: |
          pylint \
            src/api/endpoints/ec2_runner/lambda/handler.py \
            test/api/endpoints/ec2_runner/ \
            --disable=line-too-long,missing-class-docstring,missing-function-docstring,missing-module-docstring,too-many-lines \
            --fail-on=C,R,W \
            --fail-under=10.0
      - name: Static type checking Python files with mypy
        run: |
          MYPYPATH=lib python3 -m mypy src/api/endpoints/ec2_runner/lambda/handler.py
          MYPYPATH=lib python3 -m mypy --explicit-package-bases test/api/endpoints/ec2_runner/
      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ needs.declaring-common-variables.outputs.aws_region }}
          role-to-assume: ${{ needs.declaring-common-variables.outputs.github_actions_role_arn }}
      - name: Terraform Format Check
        run: cd src/api/endpoints/ec2_runner && terraform fmt -check -recursive
      - name: Terraform Init
        run: cd src/api/endpoints/ec2_runner && terraform init
      - env:
          GITHUB_TOKEN: ${{ github.token }}
        name: Run tflint
        run: |
          cd src/api/endpoints/ec2_runner && \
            tflint --init && \
            tflint
      - name: Terraform Plan
        run: cd src/api/endpoints/ec2_runner && terraform plan
  unit-testing:
    name: Unit testing
    needs:
      - declaring-common-variables
      - performing-static-analysis
    runs-on:
      - ecs-fargate-spot
      - runner-${{ github.run_id }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run unit tests
        run: |
          pytest test/api/endpoints/ec2_runner/pre_deployment/ \
            --confcutdir=test/api/endpoints/ec2_runner \
            -v
name: Deploying ec2_runner endpoint
on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/ec2_runner.yml'
      - 'src/api/endpoints/ec2_runner/**'
      - 'test/api/endpoints/ec2_runner/**'
  workflow_dispatch:
permissions:
  contents: read
  id-token: write
