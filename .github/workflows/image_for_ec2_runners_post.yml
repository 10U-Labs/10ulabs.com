concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow_ref }}-${{ github.ref }}
jobs:
  building-ami:
    name: Building AMI
    needs:
      - declaring-common-variables
      - unit-testing
    outputs:
      ami_id: ${{ steps.build.outputs.ami_id }}
      ami_stable_tag: ${{ steps.terraform.outputs.ami_stable_tag }}
      security_group_id: ${{ steps.terraform.outputs.security_group_id }}
      subnet_ids: ${{ steps.terraform.outputs.subnet_ids }}
    permissions:
      contents: read
      id-token: write
    runs-on: >-
      ${{ ( github.event.inputs.github_hosted == 'true' ||
            contains(github.event.head_commit.message, '[github-hosted]') ) &&
          'ubuntu-latest' ||
          fromJSON(format('["ecs-fargate-spot", "runner-{0}"]', github.run_id)) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ needs.declaring-common-variables.outputs.terraform_version }}
          terraform_wrapper: false
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ needs.declaring-common-variables.outputs.aws_region }}
          role-to-assume: ${{ needs.declaring-common-variables.outputs.github_actions_role_arn }}
      - id: terraform
        name: Get infrastructure info from Terraform state
        run: |
          cd src/api/backend
          terraform init -backend=true -input=false
          AMI_PURPOSE_VALUE=$(terraform output -raw ec2_runner_ami_purpose_value)
          AMI_STABLE_TAG=$(terraform output -raw ec2_runner_ami_stable_tag)
          SECURITY_GROUP_ID=$(terraform output -raw runner_security_group_id)
          SUBNET_IDS=$(terraform output -raw vpc_public_subnet_ids)
          echo "ami_purpose_value=$AMI_PURPOSE_VALUE" >> $GITHUB_OUTPUT
          echo "ami_stable_tag=$AMI_STABLE_TAG" >> $GITHUB_OUTPUT
          echo "security_group_id=$SECURITY_GROUP_ID" >> $GITHUB_OUTPUT
          echo "subnet_ids=$SUBNET_IDS" >> $GITHUB_OUTPUT
      - if: github.event.inputs.github_hosted == 'true' || contains(github.event.head_commit.message, '[github-hosted]')
        name: Install paramiko
        run: python3 -m pip install paramiko
      - env:
          OS_ARCH: ${{ needs.declaring-common-variables.outputs.os_architecture }}
          OS_FAMILY: ${{ needs.declaring-common-variables.outputs.os_family }}
          OS_VERSION: ${{ needs.declaring-common-variables.outputs.os_version }}
          PYTHONPATH: lib
        id: build
        name: Build AMI with EC2 Fleet
        run: |
          TIMESTAMP=$(date +%Y%m%d%H%M%S)
          AMI_NAME="ami-github-ec2-runner-${OS_FAMILY}-${OS_VERSION}-${OS_ARCH}-${TIMESTAMP}"
          python3 src/api/endpoints/image_for_ec2_runners/post/build_ami.py \
            --ami-name "$AMI_NAME" \
            --instance-types "${{ needs.declaring-common-variables.outputs.spot_instance_types }}" \
            --region "${{ needs.declaring-common-variables.outputs.aws_region }}" \
            --runner-user "${{ needs.declaring-common-variables.outputs.runner_user }}" \
            --runner-version "${{ needs.declaring-common-variables.outputs.runner_version }}" \
            --source-ami "${{ needs.declaring-common-variables.outputs.source_ami }}" \
            --subnet-ids "${{ steps.terraform.outputs.subnet_ids }}" \
            --yq-version "${{ needs.declaring-common-variables.outputs.yq_version }}" \
            --tag "${{ steps.terraform.outputs.ami_stable_tag }}=false" \
            --tag "BuildDate=${TIMESTAMP}" \
            --tag "ManagedBy=GitHubActions" \
            --tag "Name=${AMI_NAME}" \
            --tag "OSArchitecture=${OS_ARCH}" \
            --tag "OSFamily=${OS_FAMILY^}" \
            --tag "OSVersion=${OS_VERSION}" \
            --tag "Purpose=${{ steps.terraform.outputs.ami_purpose_value }}" \
            --tag "Repository=${{ github.repository }}" \
            --tag "RunId=${{ github.run_id }}" \
            --tag "Workflow=${{ github.workflow }}" \
            | tee build.log
          BUILD_EXIT_CODE=${PIPESTATUS[0]}
          if [ $BUILD_EXIT_CODE -ne 0 ]; then
            exit $BUILD_EXIT_CODE
          fi
          AMI_ID=$(grep '^AMI_ID=' build.log | cut -d= -f2)
          echo "ami_id=$AMI_ID" >> $GITHUB_OUTPUT
      - if: steps.build.outputs.ami_id != ''
        name: Output AMI details
        run: |
          echo "==================================="
          echo "GitHub Runner AMI Built Successfully"
          echo "==================================="
          echo ""
          echo "AMI ID: ${{ steps.build.outputs.ami_id }}"
          echo "Region: ${{ needs.declaring-common-variables.outputs.aws_region }}"
          echo "OS Family: ${{ needs.declaring-common-variables.outputs.os_family }}"
          echo "OS Version: ${{ needs.declaring-common-variables.outputs.os_version }}"
          echo "OS Architecture: ${{ needs.declaring-common-variables.outputs.os_architecture }}"
          echo ""
          echo "==================================="
  cleaning-up:
    name: Cleaning up
    needs:
      - declaring-common-variables
      - promoting-ami
    runs-on: >-
      ${{ ( github.event.inputs.github_hosted == 'true' ||
            contains(github.event.head_commit.message, '[github-hosted]') ) &&
          'ubuntu-latest' ||
          fromJSON(format('["ecs-fargate-spot", "runner-{0}"]', github.run_id)) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ needs.declaring-common-variables.outputs.aws_region }}
          role-to-assume: ${{ needs.declaring-common-variables.outputs.github_actions_role_arn }}
      - if: github.event.inputs.github_hosted == 'true' || contains(github.event.head_commit.message, '[github-hosted]')
        name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - if: github.event.inputs.github_hosted == 'true' || contains(github.event.head_commit.message, '[github-hosted]')
        name: Install Python dependencies
        run: python3 -m pip install boto3 pyyaml
      - name: Clean up old AMIs
        run: |
          python3 src/api/endpoints/image_for_ec2_runners/post/cleanup.py \
            --config src/api/endpoints/image_for_ec2_runners/post/config.yml \
            --region "${{ needs.declaring-common-variables.outputs.aws_region }}" \
            --ssm-parameter-name "${{ needs.promoting-ami.outputs.ami_ssm_parameter }}"
  declaring-common-variables:
    if: |
      github.ref == 'refs/heads/main' && (
        (github.event_name == 'push' && !contains(github.event.head_commit.message, '[dry run]')) ||
        (github.event_name == 'workflow_dispatch' && github.event.inputs.dry_run != 'true')
      )
    name: Declaring common variables
    outputs:
      aws_region: ${{ steps.config.outputs.aws_region }}
      github_actions_role_arn: ${{ steps.config.outputs.github_actions_role_arn }}
      os_architecture: ${{ steps.config.outputs.os_architecture }}
      os_family: ${{ steps.config.outputs.os_family }}
      os_version: ${{ steps.config.outputs.os_version }}
      runner_user: ${{ steps.config.outputs.runner_user }}
      runner_version: ${{ steps.config.outputs.runner_version }}
      source_ami: ${{ steps.config.outputs.source_ami }}
      spot_instance_types: ${{ steps.config.outputs.spot_instance_types }}
      terraform_version: ${{ steps.config.outputs.terraform_version }}
      yq_version: ${{ steps.config.outputs.yq_version }}
    runs-on: >-
      ${{ ( github.event.inputs.github_hosted == 'true' ||
            contains(github.event.head_commit.message, '[github-hosted]') ) &&
          'ubuntu-latest' ||
          fromJSON(format('["ecs-fargate-spot", "runner-{0}"]', github.run_id)) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - id: shared
        name: Extract shared configuration values
        run: |
          AWS_REGION=$(grep -A1 'output "aws_region"' lib/terraform/outputs.tf | grep 'value' | cut -d'"' -f2)
          AWS_ACCOUNT_ID=$(grep -A1 'output "aws_account_id"' lib/terraform/outputs.tf | grep 'value' | cut -d'"' -f2)
          RESOURCE_PREFIX=$(grep -A1 'output "resource_prefix"' lib/terraform/outputs.tf | grep 'value' | cut -d'"' -f2)
          GITHUB_ACTIONS_ROLE_SUFFIX=$(grep 'name_for_github_actions_role' src/bootstrap/locals.tf | sed 's/.*\${local\.resource_prefix}//' | cut -d'"' -f1)
          GITHUB_ACTIONS_ROLE_ARN="arn:aws:iam::${AWS_ACCOUNT_ID}:role/${RESOURCE_PREFIX}${GITHUB_ACTIONS_ROLE_SUFFIX}"
          TERRAFORM_VERSION=$(grep 'required_version' src/api/backend/providers.tf | cut -d'"' -f2 | sed 's/>= //')
          echo "aws_region=$AWS_REGION" >> $GITHUB_OUTPUT
          echo "github_actions_role_arn=$GITHUB_ACTIONS_ROLE_ARN" >> $GITHUB_OUTPUT
          echo "terraform_version=$TERRAFORM_VERSION" >> $GITHUB_OUTPUT
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ steps.shared.outputs.terraform_version }}
          terraform_wrapper: false
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ steps.shared.outputs.aws_region }}
          role-to-assume: ${{ steps.shared.outputs.github_actions_role_arn }}
      - id: api
        name: Get API terraform outputs
        run: |
          cd src/api/backend
          terraform init -backend=true -input=false
          OS_ARCHITECTURE=$(terraform output -raw fargate_cpu_architecture | tr '[:upper:]' '[:lower:]')
          SPOT_INSTANCE_TYPES=$(terraform output -json ec2_spot_instance_types | tr -d '[]" ')
          echo "os_architecture=$OS_ARCHITECTURE" >> $GITHUB_OUTPUT
          echo "spot_instance_types=$SPOT_INSTANCE_TYPES" >> $GITHUB_OUTPUT
      - name: Install yq
        run: |
          ARCH=$(dpkg --print-architecture)
          sudo wget -qO /usr/local/bin/yq "https://github.com/mikefarah/yq/releases/latest/download/yq_linux_${ARCH}" &&
            sudo chmod +x /usr/local/bin/yq
      - id: config
        name: Extract configuration values
        run: |
          CONFIG_FILE="src/api/endpoints/image_for_ec2_runners/post/config.yml"
          RUNNER_USER=$(yq '.runner_user' "$CONFIG_FILE")
          RUNNER_VERSION=$(yq '.runner_version' "$CONFIG_FILE")
          SOURCE_AMI=$(yq '.source_ami' "$CONFIG_FILE")
          YQ_VERSION=$(yq '.yq_version' "$CONFIG_FILE")
          OS_FAMILY=$(echo "$SOURCE_AMI" | cut -d'-' -f1)
          OS_VERSION=$(echo "$SOURCE_AMI" | cut -d'-' -f2)
          echo "aws_region=${{ steps.shared.outputs.aws_region }}" >> $GITHUB_OUTPUT
          echo "github_actions_role_arn=${{ steps.shared.outputs.github_actions_role_arn }}" >> $GITHUB_OUTPUT
          echo "os_architecture=${{ steps.api.outputs.os_architecture }}" >> $GITHUB_OUTPUT
          echo "os_family=$OS_FAMILY" >> $GITHUB_OUTPUT
          echo "os_version=$OS_VERSION" >> $GITHUB_OUTPUT
          echo "runner_user=$RUNNER_USER" >> $GITHUB_OUTPUT
          echo "runner_version=$RUNNER_VERSION" >> $GITHUB_OUTPUT
          echo "source_ami=$SOURCE_AMI" >> $GITHUB_OUTPUT
          echo 'spot_instance_types=${{ steps.api.outputs.spot_instance_types }}' >> $GITHUB_OUTPUT
          echo "terraform_version=${{ steps.shared.outputs.terraform_version }}" >> $GITHUB_OUTPUT
          echo "yq_version=$YQ_VERSION" >> $GITHUB_OUTPUT
  e2e-testing:
    name: E2E testing
    needs:
      - building-ami
      - declaring-common-variables
      - integration-testing
    runs-on: >-
      ${{ ( github.event.inputs.github_hosted == 'true' ||
            contains(github.event.head_commit.message, '[github-hosted]') ) &&
          'ubuntu-latest' ||
          fromJSON(format('["ecs-fargate-spot", "runner-{0}"]', github.run_id)) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ needs.declaring-common-variables.outputs.aws_region }}
          role-to-assume: ${{ needs.declaring-common-variables.outputs.github_actions_role_arn }}
      - if: github.event.inputs.github_hosted == 'true' || contains(github.event.head_commit.message, '[github-hosted]')
        name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - if: github.event.inputs.github_hosted == 'true' || contains(github.event.head_commit.message, '[github-hosted]')
        name: Install dependencies
        run: python3 -m pip install boto3 pytest pyyaml
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ needs.declaring-common-variables.outputs.terraform_version }}
          terraform_wrapper: false
      - name: Get GitHub PAT from Parameter Store
        run: |
          terraform -chdir=src/bootstrap init -backend=true -input=false > /dev/null
          GITHUB_TOKEN_SECRET_NAME=$(terraform -chdir=src/bootstrap output -raw ssm_parameter_name_for_github_pat)
          GITHUB_PAT=$(aws ssm get-parameter --name "$GITHUB_TOKEN_SECRET_NAME" --with-decryption --query 'Parameter.Value' --output text)
          echo "GITHUB_PAT=$GITHUB_PAT" >> $GITHUB_ENV
      - env:
          PYTHONPATH: lib:test/api/endpoints/image_for_ec2_runners/post/post_deployment
          SPOT_INSTANCE_TYPES: ${{ needs.declaring-common-variables.outputs.spot_instance_types }}
          TEST_AMI_ID: ${{ needs.building-ami.outputs.ami_id }}
          TEST_SECURITY_GROUP_ID: ${{ needs.building-ami.outputs.security_group_id }}
          TEST_SUBNET_IDS: ${{ needs.building-ami.outputs.subnet_ids }}
        name: Run E2E tests
        run: |
          pytest test/api/endpoints/image_for_ec2_runners/post/post_deployment/e2e/ \
            --confcutdir=test/api/endpoints/image_for_ec2_runners/post \
            -v
  integration-testing:
    name: Integration testing
    needs:
      - building-ami
      - declaring-common-variables
    runs-on: >-
      ${{ ( github.event.inputs.github_hosted == 'true' ||
            contains(github.event.head_commit.message, '[github-hosted]') ) &&
          'ubuntu-latest' ||
          fromJSON(format('["ecs-fargate-spot", "runner-{0}"]', github.run_id)) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ needs.declaring-common-variables.outputs.aws_region }}
          role-to-assume: ${{ needs.declaring-common-variables.outputs.github_actions_role_arn }}
      - if: github.event.inputs.github_hosted == 'true' || contains(github.event.head_commit.message, '[github-hosted]')
        name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - if: github.event.inputs.github_hosted == 'true' || contains(github.event.head_commit.message, '[github-hosted]')
        name: Install Python dependencies
        run: python3 -m pip install boto3 pytest pyyaml
      - env:
          PYTHONPATH: lib:test/api/endpoints/image_for_ec2_runners/post/post_deployment
          SPOT_INSTANCE_TYPES: ${{ needs.declaring-common-variables.outputs.spot_instance_types }}
          TEST_AMI_ID: ${{ needs.building-ami.outputs.ami_id }}
          TEST_SECURITY_GROUP_ID: ${{ needs.building-ami.outputs.security_group_id }}
          TEST_SUBNET_IDS: ${{ needs.building-ami.outputs.subnet_ids }}
        name: Run pytest integration tests
        run: |
          pytest test/api/endpoints/image_for_ec2_runners/post/post_deployment/integration/ \
            --confcutdir=test/api/endpoints/image_for_ec2_runners/post \
            -v
  performing-static-analysis:
    name: Performing static analysis
    needs: declaring-common-variables
    runs-on: >-
      ${{ ( github.event.inputs.github_hosted == 'true' ||
            contains(github.event.head_commit.message, '[github-hosted]') ) &&
          'ubuntu-latest' ||
          fromJSON(format('["ecs-fargate-spot", "runner-{0}"]', github.run_id)) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - if: github.event.inputs.github_hosted == 'true' || contains(github.event.head_commit.message, '[github-hosted]')
        name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - if: github.event.inputs.github_hosted == 'true' || contains(github.event.head_commit.message, '[github-hosted]')
        name: Install yamllint
        run: python3 -m pip install yamllint
      - name: Linting YAML files
        run: |
          DOCUMENT_START='document-start: disable'
          EMPTY_LINES='empty-lines: {max: 0, max-start: 0, max-end: 1}'
          KEY_ORDERING='key-ordering: enable'
          LINE_LENGTH='line-length: {max: 300, level: warning}'
          NEWLINE_AT_EOF='new-line-at-end-of-file: enable'
          TRUTHY='truthy: {allowed-values: ["true", "false", "on"]}'
          RULES="$DOCUMENT_START, $EMPTY_LINES, $KEY_ORDERING, $LINE_LENGTH, $NEWLINE_AT_EOF, $TRUTHY"
          yamllint \
            --config-data "{extends: default, rules: {$RULES}}" \
            .github/workflows/image_for_ec2_runners_post.yml \
            src/api/endpoints/image_for_ec2_runners/post/config.yml
      - if: github.event.inputs.github_hosted == 'true' || contains(github.event.head_commit.message, '[github-hosted]')
        name: Install Python linting dependencies
        run: python3 -m pip install boto3 boto3-stubs mypy paramiko pylint pytest types-paramiko types-PyYAML
      - env:
          PYTHONPATH: lib:test/api/endpoints/image_for_ec2_runners/post/post_deployment
        name: Linting Python files
        run: |
          pylint \
            lib/ec2_spot/*.py \
            src/api/endpoints/image_for_ec2_runners/post/*.py \
            test/api/endpoints/image_for_ec2_runners/post/ \
            --disable=line-too-long,missing-class-docstring,missing-function-docstring,missing-module-docstring,too-many-lines \
            --fail-on=C,R,W \
            --fail-under=10.0
      - name: Static type checking Python files with mypy
        run: |
          MYPYPATH=lib python3 -m mypy src/api/endpoints/image_for_ec2_runners/post/*.py
          MYPYPATH=lib:test/api/endpoints/image_for_ec2_runners/post/post_deployment python3 -m mypy --explicit-package-bases lib/ec2_spot/ test/api/endpoints/image_for_ec2_runners/post/
  promoting-ami:
    name: Promoting AMI
    needs:
      - building-ami
      - declaring-common-variables
      - e2e-testing
    outputs:
      ami_ssm_parameter: ${{ steps.config.outputs.ami_ssm_parameter }}
    permissions:
      contents: read
      id-token: write
    runs-on: >-
      ${{ ( github.event.inputs.github_hosted == 'true' ||
            contains(github.event.head_commit.message, '[github-hosted]') ) &&
          'ubuntu-latest' ||
          fromJSON(format('["ecs-fargate-spot", "runner-{0}"]', github.run_id)) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ needs.declaring-common-variables.outputs.terraform_version }}
          terraform_wrapper: false
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ needs.declaring-common-variables.outputs.aws_region }}
          role-to-assume: ${{ needs.declaring-common-variables.outputs.github_actions_role_arn }}
      - id: config
        name: Get SSM parameter name from Terraform
        run: |
          cd src/api/backend
          terraform init -backend=true -input=false
          AMI_SSM_PARAMETER=$(terraform output -raw ssm_parameter_name_for_latest_ami)
          echo "ami_ssm_parameter=$AMI_SSM_PARAMETER" >> $GITHUB_OUTPUT
      - if: github.event.inputs.github_hosted == 'true' || contains(github.event.head_commit.message, '[github-hosted]')
        name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - if: github.event.inputs.github_hosted == 'true' || contains(github.event.head_commit.message, '[github-hosted]')
        name: Install Python dependencies
        run: python3 -m pip install boto3
      - name: Promote AMI
        run: |
          python3 src/api/endpoints/image_for_ec2_runners/post/promote_ami.py \
            --ami-id "${{ needs.building-ami.outputs.ami_id }}" \
            --region "${{ needs.declaring-common-variables.outputs.aws_region }}" \
            --run-id "${{ github.run_id }}" \
            --ssm-parameter-name "${{ steps.config.outputs.ami_ssm_parameter }}" \
            --tag-key "${{ needs.building-ami.outputs.ami_stable_tag }}"
  unit-testing:
    name: Unit testing
    needs:
      - declaring-common-variables
      - performing-static-analysis
    runs-on: >-
      ${{ ( github.event.inputs.github_hosted == 'true' ||
            contains(github.event.head_commit.message, '[github-hosted]') ) &&
          'ubuntu-latest' ||
          fromJSON(format('["ecs-fargate-spot", "runner-{0}"]', github.run_id)) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - if: github.event.inputs.github_hosted == 'true' || contains(github.event.head_commit.message, '[github-hosted]')
        name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - if: github.event.inputs.github_hosted == 'true' || contains(github.event.head_commit.message, '[github-hosted]')
        name: Install Python dependencies
        run: python3 -m pip install boto3 botocore paramiko pytest pyyaml
      - name: Run unit tests
        run: |
          pytest test/api/endpoints/image_for_ec2_runners/post/pre_deployment/ \
            --confcutdir=test/api/endpoints/image_for_ec2_runners/post \
            -v
name: Building AMI for EC2 self-hosted runners
on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/image_for_ec2_runners_post.yml'
      - 'src/api/endpoints/image_for_ec2_runners/post/**'
      - 'test/api/endpoints/image_for_ec2_runners/post/**'
  workflow_dispatch:
    inputs:
      dry_run:
        default: false
        description: Dry run mode (show structure without executing)
        required: false
        type: boolean
      github_hosted:
        default: false
        description: 'Use GitHub-hosted runner instead of ephemeral ECS'
        type: boolean
permissions:
  contents: read
  id-token: write
