{
  "tools": [
    {
      "name": "list_test_directories",
      "description": "List all pre-deployment integration test directories in the repository",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    {
      "name": "read_file",
      "description": "Read a file from the repository",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Path to the file in the repository"
          }
        },
        "required": ["path"]
      }
    },
    {
      "name": "read_terraform_data",
      "description": "Read terraform data.tf to identify dependencies for a given directory",
      "inputSchema": {
        "type": "object",
        "properties": {
          "directory": {
            "type": "string",
            "description": "Path to the terraform directory (e.g., src/api/backend)"
          }
        },
        "required": ["directory"]
      }
    },
    {
      "name": "analyze_test_compliance",
      "description": "Analyze if a test file follows the five-layer testing approach",
      "inputSchema": {
        "type": "object",
        "properties": {
          "test_file_path": {
            "type": "string",
            "description": "Path to the test file to analyze"
          },
          "expected_dependencies": {
            "type": "string",
            "description": "JSON array of expected terraform dependencies"
          }
        },
        "required": ["test_file_path", "expected_dependencies"]
      }
    },
    {
      "name": "create_pull_request",
      "description": "Create a pull request with test fixes",
      "inputSchema": {
        "type": "object",
        "properties": {
          "branch_name": {
            "type": "string",
            "description": "Name for the new branch"
          },
          "title": {
            "type": "string",
            "description": "Pull request title"
          },
          "body": {
            "type": "string",
            "description": "Pull request description"
          },
          "files": {
            "type": "string",
            "description": "JSON object mapping file paths to their new contents"
          }
        },
        "required": ["branch_name", "title", "body", "files"]
      }
    },
    {
      "name": "get_documented_approach",
      "description": "Get the documented five-layer testing approach from the repository"
    }
  ]
}
